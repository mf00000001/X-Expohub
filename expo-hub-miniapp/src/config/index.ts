/**
 * 全局配置
 *
 * 后端地址说明（注意：所有接口都挂在 /api 前缀下）：
 * - 微信开发者工具(电脑端)：勾选「不校验合法域名」后，localhost:8002 可直接访问
 * - 真机预览：把 localhost 换成电脑在局域网内的 IP，如 http://192.168.1.100:8002
 * - 上线：换成已备案的 HTTPS 域名，如 https://api.expohub.com
 */
export const BASE_URL = 'http://localhost:8002/api'

/** 应用展示名 */
export const APP_NAME = '展会市场'
