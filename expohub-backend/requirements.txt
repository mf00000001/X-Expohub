# ExpoHub 后端环境 —— 固定版本 (pinned)
# ---------------------------------------------------------------
# 由本机"已验证可运行"的虚拟环境导出 (python -m pip freeze)，
# 已剔除 hermes-agent(代理运行环境, 非项目依赖)。
# 目标运行时: Python 3.13 · FastAPI + SQLAlchemy + SQLite(aiosqlite) + JWT
# MySQL / Redis 为可选部署项, 项目默认使用 SQLite。
# ---------------------------------------------------------------
# 在另一台电脑从零复现环境:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   pip install -r requirements.txt
#   python -m uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload
# ---------------------------------------------------------------
aiosqlite==0.22.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
bcrypt==5.0.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
ecdsa==0.19.2
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
passlib==1.7.4
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
python-dotenv==1.2.3
python-jose==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
rsa==4.9.1
six==1.17.0
SQLAlchemy==2.0.52
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
watchfiles==1.2.0
websockets==17.1
